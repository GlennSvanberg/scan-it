# Same as web: Vite `envDir` is the monorepo root, so root `.env.local` applies here too.
# For phone QR codes, VITE_PAIRING_ORIGIN must be the URL where the web app is served
# (e.g. https://your-app.vercel.app or your LAN dev URL).

VITE_CONVEX_URL=
VITE_PAIRING_ORIGIN=
